version: "3.8"

x-affine-image: &affine_image
  image: affine:local

services:
  validator:
    <<: *affine_image
    build:
      context: .
      dockerfile: Dockerfile
  runner:
    <<: *affine_image
  signer:
    <<: *affine_image 